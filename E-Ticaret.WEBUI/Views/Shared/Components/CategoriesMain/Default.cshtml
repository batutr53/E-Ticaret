@using E_Ticaret.Core.Entities
@using E_Ticaret.WEBUI.Helpers
@model IEnumerable<Category>

@foreach (var item in Model)
{
    <div class="menu-item">
        <a href="/kategori/@item.Id/@SeoSlugHelper.Generate(item.Name)">@item.Name</a>
    </div>

}

