@using E_Ticaret.Core.Entities
@model IEnumerable<Category>

@foreach (var item in Model)
{
    <div class="menu-item">
        <a href="/Category?id=@item.Id"><i class="fa-solid @item.ClassImage"></i>@item.Name</a>
    </div>

}

