@model E_Ticaret.Core.Entities.ContentPage
@{
    ViewData["Title"] = Model.Title;
}
<section class="content-page">
    <h1>@Model.Title</h1>
    <div class="content-page-body">@Html.Raw(Model.BodyHtml)</div>
</section>
